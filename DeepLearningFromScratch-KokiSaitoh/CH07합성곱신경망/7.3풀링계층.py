# 7.3 풀링 계층
"""
풀링 계층(Pooling Layer)의 개념
- 세로 및 가로 방향의 공간 차원을 줄이는 연산임
- 특정 크기의 영역을 설정하여 해당 영역의 데이터들을 원소 하나로 집약함

최대 풀링(Max Pooling)의 작동 방식
- 대상 영역 내에서 가장 큰 최댓값을 구하는 연산임
- 예시로 (2, 2) 크기의 풀링 윈도우를 스트라이드 2로 처리할 시, 2x2 정사각형 범위 내에서 가장 큰 값 하나를 꺼낸 뒤 윈도우를 2칸 이동함
- 윈도우 크기가 (n, n)이면 스트라이드도 동일하게 n으로 맞추는 것이 일반적임

풀링 계층의 3가지 핵심 특징
- 학습해야 할 매개변수가 없음: 단순히 최댓값이나 평균을 취하는 명확한 연산이므로 가중치 갱신 과정이 필요하지 않음
- 채널 수 유지: 연산이 각 채널별로 독립적으로 계산되기 때문에 입력 데이터의 채널 수가 출력 데이터로 변함없이 유지됨
- 미세한 위치 변화에 강건함(Robust): 입력 데이터가 미세하게 어긋나거나 변하더라도 풀링 연산이 이를 흡수하여 출력 결과에 미치는 영향을 최소화함
"""